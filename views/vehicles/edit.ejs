<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Edit Vehicle | AI Fleet Management</title>
</head>
<body>
    <header>
        <nav class="navbar">
            <a href="/admin/dashboard" class="navbar-brand">AI Fleet Manager</a>
            <ul class="nav-links">
                <% if (isAuthenticated) { %>
                    <li><a href="/admin/dashboard">Dashboard</a></li>
                    <li><a href="/vehicles">Vehicles</a></li>
                    <% if (user && user.role === 'admin') { %>
                        <li><a href="/admin/users">Users</a></li>
                        <li><a href="/auth/register">Create User</a></li>
                    <% } %>
                    <li><a href="/auth/logout">Logout (<%= user.username %>)</a></li>
                <% } else { %>
                    <li><a href="/auth/login">Login</a></li>
                <% } %>
            </ul>
        </nav>
    </header>

    <div class="container">
        <% if (locals.error_msg && error_msg.length > 0) { %>
            <div class="flash-message error"><%= error_msg %></div>
        <% } %>

        <h1>Edit Vehicle: <%= vehicle.make %> <%= vehicle.model %></h1>
        <form action="/vehicles/edit/<%= vehicle._id %>" method="POST">
            <div class="form-group">
                <label for="make">Make</label>
                <input type="text" id="make" name="make" class="form-control" value="<%= vehicle.make %>" required>
            </div>
            <div class="form-group">
                <label for="model">Model</label>
                <input type="text" id="model" name="model" class="form-control" value="<%= vehicle.model %>" required>
            </div>
            <div class="form-group">
                <label for="year">Year</label>
                <input type="number" id="year" name="year" class="form-control" value="<%= vehicle.year %>" required>
            </div>
            <div class="form-group">
                <label for="vin">VIN (17 characters)</label>
                <input type="text" id="vin" name="vin" class="form-control" value="<%= vehicle.vin %>" required maxlength="17" minlength="17">
            </div>
            <div class="form-group">
                <label for="licensePlate">License Plate</label>
                <input type="text" id="licensePlate" name="licensePlate" class="form-control" value="<%= vehicle.licensePlate %>" required>
            </div>
            <div class="form-group">
                <label for="capacity">Capacity (e.g., kg)</label>
                <input type="number" id="capacity" name="capacity" class="form-control" value="<%= vehicle.capacity %>">
            </div>
            <div class="form-group">
                <label for="currentMileage">Current Mileage</label>
                <input type="number" id="currentMileage" name="currentMileage" class="form-control" min="0" value="<%= vehicle.currentMileage %>">
            </div>
            <div class="form-group">
                <label for="status">Status</label>
                <select id="status" name="status" class="form-control" required>
                    <option value="active" <%= vehicle.status === 'active' ? 'selected' : '' %>>Active</option>
                    <option value="inactive" <%= vehicle.status === 'inactive' ? 'selected' : '' %>>Inactive</option>
                    <option value="in_maintenance" <%= vehicle.status === 'in_maintenance' ? 'selected' : '' %>>In Maintenance</option>
                    <option value="on_trip" <%= vehicle.status === 'on_trip' ? 'selected' : '' %>>On Trip</option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Update Vehicle</button>
            <a href="/vehicles" class="btn btn-secondary">Cancel</a>
        </form>
    </div>

    <footer>
        <div class="container">
            <p>© <%= new Date().getFullYear() %> AI Fleet & Route Optimization Management System. All rights reserved.</p>
        </div>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>