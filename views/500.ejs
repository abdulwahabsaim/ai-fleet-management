<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <title>500 - Server Error | AI Fleet Management</title>
</head>
<body>
    <div class="login-container">
        <div class="login-card text-center">
            <i class="fas fa-bug fa-4x" style="color: var(--danger); margin-bottom: 2rem;"></i>
            <h1 class="login-title">500 - Server Error</h1>
            <p style="color: var(--text-secondary-dark); margin-bottom: 1rem;">Something went wrong on our end.</p>
            
            <% if (typeof error !== 'undefined') { %>
                <div style="background-color: var(--bg-input-dark); padding: 1rem; border-radius: var(--border-radius); text-align: left; margin: 1rem 0; color: var(--danger); font-family: monospace; white-space: pre-wrap; word-wrap: break-word;">
                    <%= error %>
                </div>
            <% } %>
            
            <a href="/" class="btn btn--primary w-100">Go Home</a>
        </div>
    </div>
    
    <div class="footer">
        <p>© <%= new Date().getFullYear() %> AI Fleet & Route Optimization Management System. All rights reserved.</p>
    </div>
</body>
</html>