// This file is a placeholder. For actual production, all sensitive keys
// should be managed via environment variables and NOT hardcoded.
// We use .env for this purpose.

module.exports = {
    googleMapsApiKey: process.env.GOOGLE_MAPS_API_KEY
};